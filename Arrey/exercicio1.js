// criando um arrey de 5 numeros

const numeros = [0, 12, 35, 47, 84]

let i = 0
// enquanto i (indice) for menor que o tamanho do arrey
while (i < numeros.length) {
    // executar isso
    console.log(numeros[i])
    i++
}
