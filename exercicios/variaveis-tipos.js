let nome = "Saulo"; //string.
let idade = "38"; //number.
let nota; //underfined *quando o valor da variavel nao esta definido.
nota = null //null *quando o valor ainda não esta definido ou é nulo.
// não é aplicado crase, porque null é uma palavra reservada por sí só.

console.log(nota); //undefined
nota = 10;
console.log(nota); //10
nota = null;
console.log(nota); //null
